# Immo Eliza ML — pinned to the versions used to build the project.
# Python 3.13
pandas==2.2.3
numpy==2.2.3
scikit-learn==1.8.0
xgboost==3.3.0
joblib==1.5.3
scipy==1.17.0

# neighbourhood-priciness feature (preprocessing imports geo/) + parquet store
h3==4.5.0
shapely==2.1.2
pyarrow==24.0.0
openpyxl==3.1.5

# Note: on macOS, xgboost needs the OpenMP runtime:  brew install libomp

# Immo Eliza deployment — full dev/runtime dependencies.
# Covers BOTH the FastAPI backend and the Streamlit frontend, plus test tooling.
# For the slim API-only Docker image, see api/requirements.txt.
# For the Streamlit Cloud deployment, see streamlit/requirements.txt.
# Pinned to the versions the models were trained with (scikit-learn 1.8.0 /
# xgboost 3.3.0) so the joblib pipelines load cleanly. Python 3.13.
#
# macOS note: XGBoost needs the OpenMP runtime -> `brew install libomp`.

# --- model / data ---
scikit-learn==1.8.0
xgboost==3.3.0
joblib==1.5.3
pandas==2.2.3
numpy==2.2.3
scipy==1.17.0

# --- API (FastAPI) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# --- frontend (Streamlit) ---
streamlit==1.41.1
requests==2.33.0
pydeck==0.9.3                 # priciness heatmap (deck.gl) + map pins
altair==5.5.0                # SHAP + ROI charts
streamlit-searchbox==0.1.24  # address autocomplete typeahead

# --- neighbourhood priciness / geo / explainability / data pipeline ---
shap==0.52.0                 # feature-contribution barchart (SHAP)
h3==4.5.0                    # adaptive hex priciness surface (build time)
shapely==2.1.2               # municipality polygon dissolve (build time)
pyarrow==24.0.0              # canonical listings parquet store
openpyxl==3.1.5              # read the Statbel .xlsx investment sources

# --- tests ---
pytest==8.3.4
httpx==0.28.1                # test client + Geopunt/Photon geocoding client
